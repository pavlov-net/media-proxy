# Keep distributed binaries compatible with baseline x86-64 CPUs and the
# system linker. For a local optimized build, opt in explicitly:
# RUSTFLAGS='-C target-cpu=native -C link-arg=-fuse-ld=mold' cargo build --release
