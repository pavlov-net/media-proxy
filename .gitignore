# Rust build artifacts
/target/

# Editor / IDE
.idea/
.vscode/
.claude/

# Cargo registry credentials — never commit. Tracked .cargo/config.toml
# carries project-wide build settings (target-cpu, linker preference).
.cargo/credentials
.cargo/credentials.toml

# OS
.DS_Store
Thumbs.db
*~
*.swp

# Local test fixtures (don't accidentally commit panel captures, etc.)
*.bin
*.mp4
*.avi
*.mov
*.mkv
*.webm
